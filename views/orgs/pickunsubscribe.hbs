<h2>Choose org to unsubscribe webhook</h2>
<form class="" action="../github/unsubscribe" method="post">
<select id="org" name="org">
  {{#each orgs}}
    <option value="{{org}}">{{org}}</option>
  {{/each}}
</select>
<button>Choose</button>
</form>
